"""keyboards/ — every Telegram ReplyKeyboard and InlineKeyboard builder lives
here, organized by kind (reply.py, inline.py), so keyboard layouts are never
duplicated or scattered across feature modules."""
